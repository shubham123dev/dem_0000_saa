# Contract artifacts

`api-manifest.json` is an executable inventory of the current registered HTTP surface used by the planned Angular application.

`ui-event.schema.json` is a future normalized event envelope. It is not a statement that streaming already exists.

Examples are synthetic sandbox payloads. They contain no credentials or production data and are validated by `scripts/validate_frontend_contracts.py`.
