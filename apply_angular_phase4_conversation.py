#!/usr/bin/env python3
"""Apply Angular Phase 4: real REST Ask AI conversation experience."""

from __future__ import annotations

import argparse
import hashlib
import os
import shutil
import subprocess
import sys
from pathlib import Path

EXPECTED_BRANCH = "main"
EXPECTED_HEAD = "e7b99a0031d17fa529cb48ba66fd2d92f0f5d855"
EXPECTED_REPOSITORY = "shubham123dev/dem_0000_saa"
EXPECTED_BLOBS = {'frontend/package.json': '301c3e32fd8333f17eff8004e566085656bdb39a', 'frontend/src/app/layout/app-shell/app-shell.component.html': 'bed4053aaeadfa86f3bb8aa29b0a5a8f9cc172d8', 'frontend/src/app/layout/assistant-panel/assistant-panel.component.ts': '4f9ce4f52da54ac28a7a9fd3c2beab53c725ec10', 'frontend/src/app/layout/assistant-panel/assistant-panel.component.html': 'eab1099cbb6d789464bb487b0a887ba32822b03a', 'frontend/src/app/layout/assistant-panel/assistant-panel.component.scss': '3d7f065d658c226d1c5a0edfbad1e095b1d07687'}
REPLACEMENTS = {'frontend/package.json': '{\n  "name": "dbmr-workplace-agent-ui",\n  "version": "0.1.0",\n  "private": true,\n  "description": "Angular presentation layer for the governed DBMR Workplace Agent sandbox",\n  "engines": {\n    "node": "^20.19.0 || ^22.12.0 || ^24.0.0",\n    "npm": ">=10"\n  },\n  "scripts": {\n    "start": "ng serve --host 127.0.0.1 --port 4201 --proxy-config proxy.conf.json",\n    "build": "ng build",\n    "build:development": "ng build --configuration development",\n    "typecheck": "tsc -p tsconfig.app.json --noEmit && tsc -p tsconfig.spec.json --noEmit && tsc -p tsconfig.e2e.json --noEmit",\n    "lint": "eslint \\"src/**/*.ts\\" \\"src/**/*.html\\" \\"e2e/**/*.ts\\"",\n    "test": "ng test --watch=false",\n    "test:coverage": "ng test --watch=false --coverage",\n    "e2e": "playwright test",\n    "e2e:list": "playwright test --list",\n    "quality:boundaries": "node scripts/check-architecture-boundaries.mjs",\n    "validate:phase1": "npm run quality:boundaries && npm run typecheck && npm run lint && npm run test && npm run build && npm run e2e:list",\n    "validate:phase2": "npm run quality:boundaries && npm run typecheck && npm run lint && npm run test && npm run build && npm run e2e:list",\n    "validate:phase3": "npm run quality:boundaries && npm run typecheck && npm run lint && npm run test && npm run build && npm run e2e:list",\n    "validate:phase4": "npm run quality:boundaries && npm run typecheck && npm run lint && npm run test && npm run build && npm run e2e:list",\n    "validate:full": "npm run validate:phase4 && npm run e2e"\n  },\n  "dependencies": {\n    "@angular/cdk": "21.2.14",\n    "@angular/common": "21.2.18",\n    "@angular/compiler": "21.2.18",\n    "@angular/core": "21.2.18",\n    "@angular/forms": "21.2.18",\n    "@angular/platform-browser": "21.2.18",\n    "@angular/router": "21.2.18",\n    "rxjs": "7.8.2",\n    "tslib": "2.8.1",\n    "zod": "4.4.3"\n  },\n  "devDependencies": {\n    "@angular/build": "21.2.18",\n    "@angular/cli": "21.2.18",\n    "@angular/compiler-cli": "21.2.18",\n    "@eslint/js": "9.39.1",\n    "@playwright/test": "1.61.1",\n    "@types/node": "24.3.0",\n    "angular-eslint": "21.2.0",\n    "eslint": "9.39.1",\n    "jsdom": "27.0.1",\n    "typescript": "5.9.3",\n    "typescript-eslint": "8.46.2",\n    "vitest": "4.1.10"\n  }\n}\n', 'frontend/src/app/layout/app-shell/app-shell.component.html': '<a class="skip-link" href="#main-content">Skip to content</a>\n<div\n  class="app-shell"\n  [class.app-shell--assistant-open]="state.assistantOpen()"\n  [class.app-shell--assistant-overlay]="state.assistantOverlay()"\n  [style.--shell-assistant-current.px]="state.assistantOpen() && !state.assistantOverlay() ? state.assistantWidth() : 0"\n>\n  <div class="brand-strip" aria-hidden="true"></div>\n  <app-global-header\n    class="shell-header"\n    [sectionTitle]="sectionTitle"\n    [compact]="state.isCompact()"\n    [assistantOpen]="state.assistantOpen()"\n    (navigationPressed)="state.openSidebar()"\n    (assistantPressed)="state.toggleAssistant()"\n  />\n\n  @if (state.isCompact() && state.sidebarOpen()) {\n    <button type="button" class="shell-backdrop shell-backdrop--sidebar" aria-label="Close navigation" (click)="state.closeSidebar()"></button>\n  }\n  <app-primary-sidebar\n    class="shell-sidebar"\n    [activeSection]="state.activeSection()"\n    [compact]="state.isCompact()"\n    [open]="state.sidebarOpen()"\n    (sectionSelected)="navigate($event)"\n    (closePressed)="state.closeSidebar()"\n  />\n\n  <main id="main-content" class="shell-workspace" tabindex="-1">\n    <app-workspace-dashboard [section]="state.activeSection()" (navigate)="navigate($event)" (askAi)="askAboutCurrentSection()" />\n  </main>\n\n  @if (state.assistantOpen()) {\n    @if (state.assistantOverlay()) {\n      <button type="button" class="shell-backdrop shell-backdrop--assistant" aria-label="Close Ask AI" (click)="state.closeAssistant()"></button>\n    }\n    <section class="shell-assistant" aria-label="Ask AI panel">\n      @if (!state.assistantOverlay()) {\n        <app-assistant-resize-handle [width]="state.assistantWidth()" (widthChange)="state.setAssistantWidth($event)" />\n      }\n      <app-assistant-panel\n        [overlay]="state.assistantOverlay()"\n        [currentSection]="state.activeSection()"\n        (closePressed)="state.closeAssistant()"\n        (sectionSelected)="navigate($event)"\n      />\n    </section>\n  }\n</div>\n', 'frontend/src/app/layout/assistant-panel/assistant-panel.component.ts': "import { A11yModule } from '@angular/cdk/a11y';\nimport { ChangeDetectionStrategy, Component, EventEmitter, Input, Output, ViewChild, effect, inject, type ElementRef } from '@angular/core';\nimport { AgentConversationStore } from '../../features/assistant-conversation/agent-conversation.store';\nimport { AssistantComposerComponent } from '../../features/assistant-conversation/assistant-composer/assistant-composer.component';\nimport { AssistantMessageComponent } from '../../features/assistant-conversation/assistant-message/assistant-message.component';\nimport { UiBadgeComponent, UiCalloutComponent, UiSpinnerComponent } from '../../shared/ui';\nimport type { ShellSectionId } from '../shell/shell-navigation.model';\n\ninterface StarterPrompt { readonly label: string; readonly query: string; }\n\nconst DEFAULT_PROMPTS: readonly StarterPrompt[] = [\n  { label: 'Summarize this workspace', query: 'Give me a concise overview of this organization and its current workspace status.' },\n  { label: 'Show pending approvals', query: 'List the governed action proposals that are currently waiting for approval.' },\n  { label: 'Explain available capabilities', query: 'Summarize the workplace read tools and governed actions available to me.' }\n];\n\nconst SECTION_PROMPTS: Partial<Record<ShellSectionId, readonly StarterPrompt[]>> = {\n  users: [\n    { label: 'List active users', query: 'List active organization users with their roles and membership status.' },\n    { label: 'Explain seat coverage', query: 'Summarize assigned and available organization seats.' }\n  ],\n  reports: [\n    { label: 'Summarize report access', query: 'Summarize the reports this organization can currently access.' },\n    { label: 'Find restricted reports', query: 'Identify reports that do not currently have active organization access.' }\n  ],\n  approvals: [\n    { label: 'Review waiting proposals', query: 'List proposals that are waiting for approval and summarize their risk.' },\n    { label: 'Explain approval policy', query: 'Explain the approval requirements for the currently available governed actions.' }\n  ],\n  audit: [\n    { label: 'Summarize recent audit activity', query: 'Summarize the most recent organization audit activity and outcomes.' }\n  ],\n  settings: [\n    { label: 'List settings resources', query: 'List available workplace setting resources and their current state.' }\n  ]\n};\n\n@Component({\n  selector: 'app-assistant-panel',\n  standalone: true,\n  imports: [A11yModule, AssistantComposerComponent, AssistantMessageComponent, UiBadgeComponent, UiCalloutComponent, UiSpinnerComponent],\n  changeDetection: ChangeDetectionStrategy.OnPush,\n  templateUrl: './assistant-panel.component.html',\n  styleUrl: './assistant-panel.component.scss'\n})\nexport class AssistantPanelComponent {\n  @ViewChild('transcript') private transcript?: ElementRef<HTMLElement>;\n  @ViewChild(AssistantComposerComponent) private composer?: AssistantComposerComponent;\n  @Input() overlay = false;\n  @Input() currentSection: ShellSectionId = 'home';\n  @Output() readonly closePressed = new EventEmitter<void>();\n  @Output() readonly sectionSelected = new EventEmitter<ShellSectionId>();\n  readonly conversation = inject(AgentConversationStore);\n  readonly greeting = this.createGreeting();\n\n  constructor() {\n    effect(() => {\n      this.conversation.messages();\n      this.conversation.pending();\n      queueMicrotask(() => this.scrollToLatest());\n    });\n  }\n\n  prompts(): readonly StarterPrompt[] {\n    return SECTION_PROMPTS[this.currentSection] ?? DEFAULT_PROMPTS;\n  }\n\n  submit(text: string): void { this.conversation.submit(text); }\n  submitPrompt(prompt: StarterPrompt): void { this.conversation.submit(prompt.query); }\n  startNewConversation(): void {\n    this.conversation.clearConversation();\n    queueMicrotask(() => this.composer?.focus());\n  }\n  reviewProposal(): void { this.sectionSelected.emit('approvals'); }\n\n  private scrollToLatest(): void {\n    const element = this.transcript?.nativeElement;\n    if (element) element.scrollTop = element.scrollHeight;\n  }\n\n  private createGreeting(): string {\n    const hour = new Date().getHours();\n    return hour < 12 ? 'Good morning.' : hour < 18 ? 'Good afternoon.' : 'Good evening.';\n  }\n}\n", 'frontend/src/app/layout/assistant-panel/assistant-panel.component.html': '<aside class="assistant-panel" aria-label="Ask AI" [cdkTrapFocus]="overlay" [cdkTrapFocusAutoCapture]="overlay">\n  <header>\n    <div class="assistant-title">\n      <strong>Ask AI</strong>\n      <app-ui-badge tone="info">Final responses</app-ui-badge>\n    </div>\n    <div>\n      <button type="button" class="assistant-icon-button" aria-label="Start new conversation" title="Start new conversation" (click)="startNewConversation()">＋</button>\n      <button type="button" class="assistant-icon-button" aria-label="Close Ask AI" title="Close Ask AI" (click)="closePressed.emit()">×</button>\n    </div>\n  </header>\n\n  <div #transcript class="assistant-body ui-dot-grid" role="log" aria-label="Conversation" aria-live="polite" [attr.aria-busy]="conversation.pending()">\n    @if (!conversation.configured()) {\n      <app-ui-callout tone="danger" title="Workspace is not configured">The application needs a default organization before Ask AI can send requests.</app-ui-callout>\n    } @else if (!conversation.hasMessages()) {\n      <div class="assistant-hero">\n        <div class="assistant-cloud" aria-hidden="true"><span></span><span></span><span></span></div>\n        <h2>{{ greeting }}</h2>\n        <p>Ask a workplace question or prepare a governed action proposal.</p>\n      </div>\n      <div class="assistant-suggestions" aria-label="Starter prompts">\n        @for (prompt of prompts(); track prompt.query) {\n          <button type="button" (click)="submitPrompt(prompt)">\n            <span aria-hidden="true">✦</span>\n            <span><strong>{{ prompt.label }}</strong><small>{{ prompt.query }}</small></span>\n            <span aria-hidden="true">›</span>\n          </button>\n        }\n      </div>\n      <app-ui-callout tone="info" title="Final responses, not simulated steps">\n        The current backend returns one completed read answer, clarification, or proposal. Live activity events are not shown because no streaming endpoint exists yet.\n      </app-ui-callout>\n    } @else {\n      <div class="assistant-transcript">\n        @for (message of conversation.messages(); track message.id) {\n          <app-assistant-message\n            [message]="message"\n            [retryAvailable]="conversation.retryMessageId() === message.id"\n            (retryRequested)="conversation.retryLast()"\n            (reviewProposal)="reviewProposal()"\n          />\n        }\n        @if (conversation.pending()) {\n          <div class="assistant-pending" role="status">\n            <app-ui-spinner label="Waiting for the workplace agent" />\n            <span><strong>Waiting for the backend response</strong><small>No intermediate reasoning is being simulated.</small></span>\n          </div>\n        }\n      </div>\n    }\n  </div>\n\n  <footer>\n    <p class="session-note">History is kept only for this browser tab. Each submitted request is sent to the governed backend.</p>\n    <app-assistant-composer\n      [disabled]="!conversation.configured()"\n      [pending]="conversation.pending()"\n      [maxLength]="conversation.inputLimit()"\n      [clarificationActive]="conversation.pendingClarification() !== null"\n      (submitted)="submit($event)"\n      (stopRequested)="conversation.stopWaiting()"\n      (clarificationCancelled)="conversation.dismissClarification()"\n    />\n  </footer>\n</aside>\n', 'frontend/src/app/layout/assistant-panel/assistant-panel.component.scss': ':host { display: block; height: 100%; min-height: 0; }\n.assistant-panel { display: grid; height: 100%; min-height: 0; grid-template-rows: auto minmax(0, 1fr) auto; background: var(--ui-surface-base); }\nheader { display: flex; min-height: var(--ui-shell-header-height); align-items: center; justify-content: space-between; border-bottom: 1px solid var(--ui-border-subtle); padding: 0 var(--ui-space-3); }\nheader > div { display: flex; align-items: center; gap: var(--ui-space-2); }\n.assistant-title { display: inline-flex; align-items: center; gap: var(--ui-space-2); }\n.assistant-icon-button { display: inline-grid; width: var(--ui-control-sm); height: var(--ui-control-sm); place-items: center; border: 0; border-radius: var(--ui-radius-pill); background: transparent; color: var(--ui-text-primary); cursor: pointer; font-size: var(--ui-text-lg); }\n.assistant-icon-button:hover { background: var(--ui-surface-hover); }\n.assistant-body { min-height: 0; overflow-y: auto; padding: var(--ui-space-4); scroll-behavior: smooth; }\n.assistant-hero { display: grid; justify-items: center; gap: var(--ui-space-2); padding: var(--ui-space-10) 0 var(--ui-space-6); text-align: center; }\n.assistant-hero h2 { margin: 0; font-size: var(--ui-text-xl); }\n.assistant-hero p { max-width: 23rem; margin: 0; color: var(--ui-text-secondary); font-size: var(--ui-text-sm); }\n.assistant-cloud { position: relative; width: 7rem; height: 4rem; filter: drop-shadow(0 0.5rem 1rem color-mix(in srgb, var(--ui-brand-500) 22%, transparent)); }\n.assistant-cloud span { position: absolute; bottom: 0.4rem; border-radius: var(--ui-radius-pill); background: linear-gradient(135deg, var(--ui-brand-soft), var(--ui-brand-500)); }\n.assistant-cloud span:nth-child(1) { left: 0; width: 3rem; height: 2.3rem; }\n.assistant-cloud span:nth-child(2) { left: 1.8rem; width: 4.2rem; height: 3.9rem; }\n.assistant-cloud span:nth-child(3) { right: 0; width: 2.7rem; height: 2.6rem; }\n.assistant-suggestions { display: grid; gap: var(--ui-space-2); margin-bottom: var(--ui-space-4); }\n.assistant-suggestions button { display: grid; grid-template-columns: 1.75rem minmax(0,1fr) auto; align-items: center; gap: var(--ui-space-3); border: 1px solid var(--ui-border-subtle); border-radius: var(--ui-radius-md); background: var(--ui-surface-subtle); color: var(--ui-text-primary); padding: var(--ui-space-3); text-align: left; cursor: pointer; }\n.assistant-suggestions button:hover { border-color: var(--ui-border-default); background: var(--ui-surface-base); box-shadow: var(--ui-shadow-xs); }\n.assistant-suggestions button > span:nth-child(2) { display: grid; min-width: 0; }\n.assistant-suggestions small { color: var(--ui-text-muted); font-size: var(--ui-text-xs); overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }\n.assistant-transcript { display: grid; gap: var(--ui-space-1); }\n.assistant-pending { display: flex; align-items: center; gap: var(--ui-space-3); border-top: 1px solid var(--ui-border-subtle); padding: var(--ui-space-4) var(--ui-space-2); color: var(--ui-text-secondary); }\n.assistant-pending span { display: grid; }\n.assistant-pending small { color: var(--ui-text-muted); font-size: var(--ui-text-xs); }\nfooter { display: grid; gap: var(--ui-space-2); border-top: 1px solid var(--ui-border-subtle); background: var(--ui-surface-base); padding: var(--ui-space-3); }\n.session-note { margin: 0; color: var(--ui-text-muted); font-size: var(--ui-text-xs); line-height: var(--ui-leading-normal); }\n'}
NEW_FILES = {'frontend/docs/PHASE_4_ACCEPTANCE.md': '# Phase 4 acceptance\n\n- Ask AI sends requests only through `WorkplaceAgentApiService.query`.\n- Read, clarification, and action-proposal modes render correctly.\n- No raw evidence ID, tool name, proposal ID, organization ID, or actor ID is displayed or stored.\n- Clarification replies include visible original-request context and remain within 4,000 characters.\n- Conversation history is current-tab `sessionStorage`, not fake server persistence.\n- Enter sends, Shift+Enter inserts a line, and the composer enforces backend limits.\n- Pending requests can stop browser waiting without claiming server cancellation.\n- No automatic retry, fake streaming, fake planning stage, or raw chain-of-thought exists.\n- Proposal cards navigate to Pending approvals and do not approve or execute.\n- Strict TypeScript, Angular templates, ESLint, Vitest, production build, Playwright, Phase 0–4 validators, and the complete backend suite pass.\n', 'frontend/docs/PHASE_4_CONVERSATION.md': '# Phase 4 Ask AI conversation\n\nPhase 4 connects the existing Angular shell to the current REST agent endpoint.\n\n## Current transport truth\n\nThe backend accepts one `query` and returns one final response. It does not expose a conversation ID, persisted history, SSE, WebSocket, token stream, or live planning trace. The UI therefore shows a clear pending state and then renders the final response; it never fabricates intermediate reasoning.\n\n## Supported response modes\n\n- `read`: assistant answer plus a safe count of verified backend sources.\n- `clarification_required`: assistant question and human-readable missing fields. The next reply is transparently combined with the original request because the backend endpoint is stateless.\n- `action_proposal`: safe proposal summary with risk, status, expiration, and bounded change summaries. Review opens Pending approvals; Phase 4 does not approve or execute from chat.\n\n## Persistence boundary\n\nNormalized messages are stored in `sessionStorage` for the current browser tab only. Raw evidence IDs, tool names, organization IDs, proposal IDs, API result payloads, and actor IDs are not persisted or displayed. Closing the tab clears browser history; no server-side conversation history is claimed.\n\n## Request safety\n\nRequests are never automatically retried. Stopping a request only stops the browser from waiting and explicitly warns that the backend may have completed the read or proposal. Manual retries warn users to check Pending approvals first.\n', 'frontend/e2e/assistant-conversation.spec.ts': "import { expect, type Page, test } from '@playwright/test';\n\nasync function openAssistant(page: Page) {\n  const panel=page.getByRole('complementary',{name:'Ask AI'});\n  if (await panel.count() === 0) await page.getByRole('button',{name:/Ask AI/}).first().click();\n  await expect(panel).toBeVisible();\n  return panel;\n}\n\ntest('submits a real REST query and renders a normalized read answer', async ({page}) => {\n  await page.route('**/api/workplace/organizations/**/agent/query', async (route) => {\n    const body=route.request().postDataJSON() as {query:string};\n    expect(body.query).toBe('List active users');\n    await route.fulfill({status:200,contentType:'application/json',body:JSON.stringify({mode:'read',organization_id:'org_sandbox_001',answer:'There are twelve active users.',evidence_ids:['evidence_internal'],answer_source:'model',results:[{tool_name:'internal_tool',data:{count:12}}],action_proposal:null,missing_fields:[]})});\n  });\n  await page.goto('/');\n  const panel=await openAssistant(page);\n  const composer=panel.getByRole('textbox',{name:'Ask the workplace agent'});\n  await composer.fill('List active users');\n  await panel.getByRole('button',{name:'Send'}).click();\n  await expect(panel.getByText('There are twelve active users.')).toBeVisible();\n  await expect(panel.getByText('1 verified source')).toBeVisible();\n  await expect(panel).not.toContainText('evidence_internal');\n  await expect(panel).not.toContainText('internal_tool');\n  await page.reload();\n  await expect((await openAssistant(page)).getByText('There are twelve active users.')).toBeVisible();\n});\n\ntest('replays clarification context without pretending the backend has conversation memory', async ({page}) => {\n  let call=0;\n  await page.route('**/api/workplace/organizations/**/agent/query', async (route) => {\n    call += 1;\n    const body=route.request().postDataJSON() as {query:string};\n    if (call === 1) {\n      await route.fulfill({status:200,contentType:'application/json',body:JSON.stringify({mode:'clarification_required',organization_id:'org_sandbox_001',answer:'Which report should receive access?',evidence_ids:[],answer_source:'deterministic',results:[],action_proposal:null,missing_fields:['report_id']})});\n      return;\n    }\n    expect(body.query).toContain('Original request:');\n    expect(body.query).toContain('Additional details from the user:');\n    await route.fulfill({status:200,contentType:'application/json',body:JSON.stringify({mode:'read',organization_id:'org_sandbox_001',answer:'The requested report is available for review.',evidence_ids:[],answer_source:'deterministic',results:[],action_proposal:null,missing_fields:[]})});\n  });\n  await page.goto('/');\n  const panel=await openAssistant(page);\n  const composer=panel.getByRole('textbox',{name:'Ask the workplace agent'});\n  await composer.fill('Grant report access');\n  await panel.getByRole('button',{name:'Send'}).click();\n  await expect(panel.getByText('More detail is required')).toBeVisible();\n  await composer.fill('Use the quarterly market report');\n  await panel.getByRole('button',{name:'Send'}).click();\n  await expect(panel.getByText('The requested report is available for review.')).toBeVisible();\n});\n\ntest('renders governed proposal review without executing it', async ({page}) => {\n  await page.route('**/api/workplace/organizations/**/agent/query', async (route) => {\n    await route.fulfill({status:200,contentType:'application/json',body:JSON.stringify({mode:'action_proposal',organization_id:'org_sandbox_001',answer:'A reviewable onboarding proposal is ready.',evidence_ids:[],answer_source:'deterministic',results:[],missing_fields:[],action_proposal:{id:'proposal_internal',action_name:'onboard_organization_user',risk_level:'medium',status:'pending_approval',changes:[{field:'membership.role',before:null,after:'sandbox_reader'}],expires_at:'2026-07-20T12:00:00Z'}})});\n  });\n  await page.goto('/');\n  const panel=await openAssistant(page);\n  await panel.getByRole('textbox',{name:'Ask the workplace agent'}).fill('Onboard a reader');\n  await panel.getByRole('button',{name:'Send'}).click();\n  await expect(panel.getByText('Review required')).toBeVisible();\n  await expect(panel).not.toContainText('proposal_internal');\n  await panel.getByRole('button',{name:'Review pending approvals'}).click();\n  await expect(page.getByRole('heading',{name:'Pending approvals'})).toBeVisible();\n});\n", 'frontend/src/app/features/assistant-conversation/agent-conversation.model.ts': "import { z } from 'zod';\n\nexport const conversationProposalChangeSchema = z.object({\n  fieldLabel: z.string().min(1).max(160),\n  beforeSummary: z.string().max(180),\n  afterSummary: z.string().max(180)\n}).strict();\n\nexport const conversationProposalSchema = z.object({\n  actionLabel: z.string().min(1).max(200),\n  riskLevel: z.enum(['low', 'medium', 'high']),\n  statusLabel: z.string().min(1).max(120),\n  expiresAt: z.iso.datetime({ offset: true }),\n  changes: z.array(conversationProposalChangeSchema).max(8)\n}).strict();\n\nexport const conversationMessageSchema = z.object({\n  id: z.string().min(1),\n  role: z.enum(['user', 'assistant', 'notice', 'error']),\n  text: z.string().max(8000),\n  createdAt: z.iso.datetime({ offset: true }),\n  mode: z.enum(['read', 'action_proposal', 'clarification_required']).nullable(),\n  answerSource: z.enum(['model', 'deterministic']).nullable(),\n  sourceCount: z.number().int().nonnegative(),\n  missingFields: z.array(z.string().min(1).max(160)).max(20),\n  proposal: conversationProposalSchema.nullable(),\n  tone: z.enum(['info', 'warning', 'danger']).nullable(),\n  title: z.string().max(160).nullable(),\n  retryable: z.boolean(),\n  contextNote: z.string().max(240).nullable()\n}).strict();\n\nexport const pendingClarificationSchema = z.object({\n  originalRequest: z.string().min(1).max(4000),\n  collectedDetails: z.array(z.string().min(1).max(1200)).max(8),\n  question: z.string().min(1).max(4000),\n  missingFields: z.array(z.string().min(1).max(160)).max(20)\n}).strict();\n\nexport const conversationSnapshotSchema = z.object({\n  version: z.literal(1),\n  organizationScope: z.string().min(1).max(24),\n  messages: z.array(conversationMessageSchema).max(60),\n  pendingClarification: pendingClarificationSchema.nullable()\n}).strict();\n\nexport type ConversationProposal = z.infer<typeof conversationProposalSchema>;\nexport type ConversationMessage = z.infer<typeof conversationMessageSchema>;\nexport type PendingClarification = z.infer<typeof pendingClarificationSchema>;\nexport type ConversationSnapshot = z.infer<typeof conversationSnapshotSchema>;\n\nexport function emptyConversationMessage(\n  role: ConversationMessage['role'],\n  text: string,\n  id: string,\n  createdAt: string\n): ConversationMessage {\n  return {\n    id,\n    role,\n    text,\n    createdAt,\n    mode: null,\n    answerSource: null,\n    sourceCount: 0,\n    missingFields: [],\n    proposal: null,\n    tone: null,\n    title: null,\n    retryable: false,\n    contextNote: null\n  };\n}\n", 'frontend/src/app/features/assistant-conversation/agent-conversation.store.spec.ts': "import { DOCUMENT } from '@angular/common';\nimport { TestBed } from '@angular/core/testing';\nimport { of, Subject } from 'rxjs';\nimport { beforeEach, describe, expect, it, vi } from 'vitest';\nimport { WorkplaceAgentApiService } from '../../core/api/workplace-agent-api.service';\nimport { APP_RUNTIME_CONFIG } from '../../core/config/app-config.token';\nimport type { AgentQueryResponse } from '../../core/api/wire.models';\nimport { AgentConversationStore, composeClarificationQuery } from './agent-conversation.store';\n\nconst config = { apiBaseUrl:'/api', defaultOrganizationId:'org_1', mockUserId:'usr_1', requestTimeoutMs:30000, enableDebugViews:false, streamTransport:'rest' as const };\nconst readResponse: AgentQueryResponse = { mode:'read', organization_id:'org_1', answer:'There are twelve active users.', evidence_ids:['evidence_internal'], answer_source:'model', results:[{tool_name:'internal_tool',data:{count:12}}], action_proposal:null, missing_fields:[] };\n\ndescribe('AgentConversationStore', () => {\n  beforeEach(() => { TestBed.resetTestingModule(); sessionStorage.clear(); });\n\n  it('submits through the single API facade and persists only normalized messages', () => {\n    const api = { query: vi.fn(() => of(readResponse)) };\n    TestBed.configureTestingModule({ providers:[AgentConversationStore,{provide:WorkplaceAgentApiService,useValue:api},{provide:APP_RUNTIME_CONFIG,useValue:config},{provide:DOCUMENT,useValue:document}] });\n    const store=TestBed.inject(AgentConversationStore);\n    store.submit('List active users');\n    TestBed.tick();\n    expect(api.query).toHaveBeenCalledWith('org_1','List active users');\n    expect(store.messages().map((message)=>message.role)).toEqual(['user','assistant']);\n    const saved=sessionStorage.getItem('dbmr-workplace-conversation-v1') ?? '';\n    expect(saved).not.toContain('evidence_internal');\n    expect(saved).not.toContain('internal_tool');\n    expect(saved).not.toContain('org_1');\n  });\n\n  it('sends clarification replies with transparent original-request context', () => {\n    const clarification: AgentQueryResponse = { mode:'clarification_required', organization_id:'org_1', answer:'Which report should receive access?', evidence_ids:[], answer_source:'deterministic', results:[], action_proposal:null, missing_fields:['report_id'] };\n    const api = { query: vi.fn().mockReturnValueOnce(of(clarification)).mockReturnValueOnce(of(readResponse)) };\n    TestBed.configureTestingModule({ providers:[AgentConversationStore,{provide:WorkplaceAgentApiService,useValue:api},{provide:APP_RUNTIME_CONFIG,useValue:config},{provide:DOCUMENT,useValue:document}] });\n    const store=TestBed.inject(AgentConversationStore);\n    store.submit('Grant report access');\n    store.submit('Use the quarterly market report');\n    const secondQuery=api.query.mock.calls[1]?.[1] as string;\n    expect(secondQuery).toContain('Original request:');\n    expect(secondQuery).toContain('Additional details from the user:');\n    expect(secondQuery.length).toBeLessThanOrEqual(4000);\n  });\n\n  it('stops waiting without claiming the backend operation was cancelled', () => {\n    const pending = new Subject<AgentQueryResponse>();\n    const api = { query: vi.fn(() => pending.asObservable()) };\n    TestBed.configureTestingModule({ providers:[AgentConversationStore,{provide:WorkplaceAgentApiService,useValue:api},{provide:APP_RUNTIME_CONFIG,useValue:config},{provide:DOCUMENT,useValue:document}] });\n    const store=TestBed.inject(AgentConversationStore);\n    store.submit('Summarize audit activity');\n    store.stopWaiting();\n    expect(store.pending()).toBe(false);\n    expect(store.messages().at(-1)?.text).toContain('backend may still have completed');\n  });\n\n  it('keeps composed clarification requests within the backend limit', () => {\n    const query=composeClarificationQuery({originalRequest:'x'.repeat(4000),collectedDetails:['y'.repeat(1200)],question:'z'.repeat(4000),missingFields:['report_id']},'r'.repeat(1200));\n    expect(query.length).toBeLessThanOrEqual(4000);\n  });\n});\n", 'frontend/src/app/features/assistant-conversation/agent-conversation.store.ts': "import { DOCUMENT } from '@angular/common';\nimport { computed, DestroyRef, effect, inject, Injectable, signal } from '@angular/core';\nimport type { Subscription } from 'rxjs';\nimport { WorkplaceAgentApiService } from '../../core/api/workplace-agent-api.service';\nimport { APP_RUNTIME_CONFIG } from '../../core/config/app-config.token';\nimport { normalizeWorkplaceError } from '../../core/errors/error-normalizer';\nimport type { ConversationMessage, ConversationSnapshot, PendingClarification } from './agent-conversation.model';\nimport { conversationSnapshotSchema, emptyConversationMessage } from './agent-conversation.model';\nimport { mapAgentResponse } from './agent-response.mapper';\n\nconst STORAGE_KEY = 'dbmr-workplace-conversation-v1';\nconst SNAPSHOT_VERSION = 1;\nconst MAX_MESSAGES = 60;\nconst MAX_SNAPSHOT_BYTES = 220_000;\nexport const CLARIFICATION_REPLY_LIMIT = 1200;\n\ninterface SubmissionRecord {\n  readonly displayText: string;\n  readonly apiQuery: string;\n  readonly rootOriginal: string;\n  readonly collectedDetails: readonly string[];\n  readonly contextNote: string | null;\n}\n\n\nfunction createOrganizationScope(value: string): string {\n  let hash = 2166136261;\n  for (const character of value) {\n    hash ^= character.charCodeAt(0);\n    hash = Math.imul(hash, 16777619);\n  }\n  return `scope-${(hash >>> 0).toString(16)}`;\n}\n\nfunction truncate(value: string, limit: number): string {\n  const compact = value.trim();\n  return compact.length <= limit ? compact : `${compact.slice(0, limit - 1)}…`;\n}\n\nexport function composeClarificationQuery(context: PendingClarification, reply: string): string {\n  const previousDetails = context.collectedDetails.length\n    ? context.collectedDetails.map((detail, index) => `${index + 1}. ${detail}`).join('\\n')\n    : 'None yet.';\n  const query = [\n    'Original request:',\n    truncate(context.originalRequest, 1300),\n    '',\n    'Previous clarification details:',\n    truncate(previousDetails, 600),\n    '',\n    'Clarification requested by the workplace agent:',\n    truncate(context.question, 500),\n    '',\n    'Additional details from the user:',\n    truncate(reply, CLARIFICATION_REPLY_LIMIT),\n    '',\n    'Re-evaluate the original request using these details. Ask another clarification only when a required field is still missing.'\n  ].join('\\n');\n  if (query.length > 4000) throw new Error('Clarification context exceeds the backend request limit.');\n  return query;\n}\n\n@Injectable({ providedIn: 'root' })\nexport class AgentConversationStore {\n  private readonly document = inject(DOCUMENT);\n  private readonly destroyRef = inject(DestroyRef);\n  private readonly config = inject(APP_RUNTIME_CONFIG);\n  private readonly api = inject(WorkplaceAgentApiService);\n  private readonly organizationId = this.config.defaultOrganizationId;\n  private readonly organizationScope = this.organizationId ? createOrganizationScope(this.organizationId) : null;\n  private readonly initialSnapshot = this.readSnapshot();\n  private readonly messagesState = signal<ConversationMessage[]>(this.initialSnapshot?.messages ?? []);\n  private readonly pendingState = signal(false);\n  private readonly clarificationState = signal<PendingClarification | null>(this.initialSnapshot?.pendingClarification ?? null);\n  private readonly retryMessageIdState = signal<string | null>(null);\n  private activeRequest: Subscription | null = null;\n  private lastSubmission: SubmissionRecord | null = null;\n\n  readonly messages = this.messagesState.asReadonly();\n  readonly pending = this.pendingState.asReadonly();\n  readonly pendingClarification = this.clarificationState.asReadonly();\n  readonly retryMessageId = this.retryMessageIdState.asReadonly();\n  readonly hasMessages = computed(() => this.messages().length > 0);\n  readonly inputLimit = computed(() => this.pendingClarification() ? CLARIFICATION_REPLY_LIMIT : 4000);\n  readonly configured = computed(() => Boolean(this.organizationId));\n  readonly canSubmit = computed(() => this.configured() && !this.pending());\n\n  constructor() {\n    effect(() => this.persistSnapshot(this.messages(), this.pendingClarification()));\n    this.destroyRef.onDestroy(() => this.activeRequest?.unsubscribe());\n  }\n\n  submit(text: string): void {\n    const normalized = text.trim();\n    if (!normalized || !this.canSubmit() || normalized.length > this.inputLimit()) return;\n    const clarification = this.pendingClarification();\n    const submission: SubmissionRecord = clarification\n      ? {\n          displayText: normalized,\n          apiQuery: composeClarificationQuery(clarification, normalized),\n          rootOriginal: clarification.originalRequest,\n          collectedDetails: [...clarification.collectedDetails, normalized],\n          contextNote: 'Sent together with the original request and earlier clarification details.'\n        }\n      : {\n          displayText: normalized,\n          apiQuery: normalized,\n          rootOriginal: normalized,\n          collectedDetails: [],\n          contextNote: null\n        };\n    this.send(submission);\n  }\n\n  retryLast(): void {\n    if (!this.lastSubmission || this.pending()) return;\n    this.send({\n      ...this.lastSubmission,\n      contextNote: 'Retried manually. Check Pending approvals if the earlier request may have completed.'\n    });\n  }\n\n  stopWaiting(): void {\n    if (!this.pending()) return;\n    this.activeRequest?.unsubscribe();\n    this.activeRequest = null;\n    this.pendingState.set(false);\n    this.retryMessageIdState.set(null);\n    this.append({\n      ...emptyConversationMessage(\n        'notice',\n        'Stopped waiting for this response. The backend may still have completed the read or prepared a proposal, so check Pending approvals before repeating the request.',\n        this.createId(),\n        this.now()\n      ),\n      tone: 'warning',\n      title: 'Browser request stopped'\n    });\n  }\n\n  dismissClarification(): void {\n    this.clarificationState.set(null);\n  }\n\n  clearConversation(): void {\n    this.activeRequest?.unsubscribe();\n    this.activeRequest = null;\n    this.lastSubmission = null;\n    this.pendingState.set(false);\n    this.retryMessageIdState.set(null);\n    this.clarificationState.set(null);\n    this.messagesState.set([]);\n    this.removeSnapshot();\n  }\n\n  private send(submission: SubmissionRecord): void {\n    if (!this.organizationId) return;\n    this.activeRequest?.unsubscribe();\n    this.lastSubmission = submission;\n    this.retryMessageIdState.set(null);\n    this.append({\n      ...emptyConversationMessage('user', submission.displayText, this.createId(), this.now()),\n      contextNote: submission.contextNote\n    });\n    this.pendingState.set(true);\n\n    this.activeRequest = this.api.query(this.organizationId, submission.apiQuery).subscribe({\n      next: (response) => {\n        this.append(mapAgentResponse(response, this.createId(), this.now()));\n        if (response.mode === 'clarification_required') {\n          this.clarificationState.set({\n            originalRequest: submission.rootOriginal,\n            collectedDetails: [...submission.collectedDetails].slice(-8),\n            question: truncate(response.answer, 4000),\n            missingFields: response.missing_fields.slice(0, 20).map((field) => truncate(field, 160))\n          });\n        } else {\n          this.clarificationState.set(null);\n        }\n      },\n      error: (error: unknown) => {\n        const normalized = normalizeWorkplaceError(error);\n        const messageId = this.createId();\n        this.append({\n          ...emptyConversationMessage('error', normalized.message, messageId, this.now()),\n          tone: 'danger',\n          title: normalized.title,\n          retryable: normalized.retryable\n        });\n        this.retryMessageIdState.set(normalized.retryable ? messageId : null);\n        this.pendingState.set(false);\n        this.activeRequest = null;\n      },\n      complete: () => {\n        this.pendingState.set(false);\n        this.activeRequest = null;\n      }\n    });\n  }\n\n  private append(message: ConversationMessage): void {\n    this.messagesState.update((messages) => [...messages, message].slice(-MAX_MESSAGES));\n  }\n\n  private now(): string {\n    return new Date().toISOString();\n  }\n\n  private createId(): string {\n    return globalThis.crypto?.randomUUID?.() ?? `message-${Date.now()}-${Math.random().toString(16).slice(2)}`;\n  }\n\n  private readSnapshot(): ConversationSnapshot | null {\n    if (!this.organizationScope) return null;\n    try {\n      const raw = this.document.defaultView?.sessionStorage.getItem(STORAGE_KEY);\n      if (!raw) return null;\n      const parsed: unknown = JSON.parse(raw);\n      const snapshot = conversationSnapshotSchema.parse(parsed);\n      return snapshot.organizationScope === this.organizationScope ? snapshot : null;\n    } catch {\n      this.removeSnapshot();\n      return null;\n    }\n  }\n\n  private persistSnapshot(messages: readonly ConversationMessage[], pendingClarification: PendingClarification | null): void {\n    if (!this.organizationScope) return;\n    const snapshot: ConversationSnapshot = {\n      version: SNAPSHOT_VERSION,\n      organizationScope: this.organizationScope,\n      messages: [...messages].slice(-MAX_MESSAGES),\n      pendingClarification\n    };\n    try {\n      let encoded = JSON.stringify(snapshot);\n      if (encoded.length > MAX_SNAPSHOT_BYTES) {\n        encoded = JSON.stringify({ ...snapshot, messages: snapshot.messages.slice(-20) });\n      }\n      this.document.defaultView?.sessionStorage.setItem(STORAGE_KEY, encoded);\n    } catch {\n      // Session storage is an optional convenience and never blocks the agent.\n    }\n  }\n\n  private removeSnapshot(): void {\n    try {\n      this.document.defaultView?.sessionStorage.removeItem(STORAGE_KEY);\n    } catch {\n      // Storage may be disabled.\n    }\n  }\n}\n", 'frontend/src/app/features/assistant-conversation/agent-response.mapper.spec.ts': "import { describe, expect, it } from 'vitest';\nimport type { AgentQueryResponse } from '../../core/api/wire.models';\nimport { humanizeIdentifier, mapAgentResponse, summarizeProposalValue } from './agent-response.mapper';\n\ndescribe('agent response mapper', () => {\n  it('maps a proposal without exposing proposal or evidence identifiers', () => {\n    const response: AgentQueryResponse = {\n      mode: 'action_proposal', organization_id: 'org_internal', answer: 'A proposal is ready.', evidence_ids: [], answer_source: 'deterministic', results: [], missing_fields: [],\n      action_proposal: { id: 'proposal_internal', action_name: 'onboard_organization_user', risk_level: 'medium', status: 'pending_approval', expires_at: '2026-07-19T12:00:00Z', changes: [{ field: 'membership.role', before: null, after: 'sandbox_reader' }] }\n    };\n    const mapped = mapAgentResponse(response, 'message-1', '2026-07-19T10:00:00Z');\n    expect(mapped.proposal?.actionLabel).toBe('Onboard organization user');\n    expect(JSON.stringify(mapped)).not.toContain('proposal_internal');\n    expect(JSON.stringify(mapped)).not.toContain('org_internal');\n  });\n\n  it('humanizes identifiers and summarizes structured values safely', () => {\n    expect(humanizeIdentifier('report_access.status')).toBe('Report access status');\n    expect(summarizeProposalValue({ secret: 'value' })).toBe('Structured value');\n  });\n});\n", 'frontend/src/app/features/assistant-conversation/agent-response.mapper.ts': "import type { AgentQueryResponse } from '../../core/api/wire.models';\nimport type { ConversationMessage, ConversationProposal } from './agent-conversation.model';\nimport { emptyConversationMessage } from './agent-conversation.model';\n\nconst VALUE_LIMIT = 140;\n\nexport function humanizeIdentifier(value: string): string {\n  const normalized = value.replace(/[._-]+/g, ' ').replace(/\\s+/g, ' ').trim();\n  if (!normalized) return 'Requested detail';\n  return normalized.charAt(0).toUpperCase() + normalized.slice(1);\n}\n\nexport function summarizeProposalValue(value: unknown): string {\n  if (value === null || value === undefined || value === '') return 'Not set';\n  if (typeof value === 'boolean') return value ? 'Yes' : 'No';\n  if (typeof value === 'number' || typeof value === 'bigint') return String(value);\n  if (typeof value === 'string') {\n    const compact = value.replace(/\\s+/g, ' ').trim();\n    return compact.length <= VALUE_LIMIT ? compact : `${compact.slice(0, VALUE_LIMIT - 1)}…`;\n  }\n  if (Array.isArray(value)) return `${value.length} item${value.length === 1 ? '' : 's'}`;\n  return 'Structured value';\n}\n\nfunction mapProposal(response: AgentQueryResponse): ConversationProposal | null {\n  const proposal = response.action_proposal;\n  if (!proposal) return null;\n  return {\n    actionLabel: humanizeIdentifier(proposal.action_name),\n    riskLevel: proposal.risk_level,\n    statusLabel: humanizeIdentifier(proposal.status),\n    expiresAt: proposal.expires_at,\n    changes: proposal.changes.slice(0, 8).map((change) => ({\n      fieldLabel: humanizeIdentifier(change.field),\n      beforeSummary: summarizeProposalValue(change.before),\n      afterSummary: summarizeProposalValue(change.after)\n    }))\n  };\n}\n\nexport function mapAgentResponse(\n  response: AgentQueryResponse,\n  id: string,\n  createdAt: string\n): ConversationMessage {\n  const answer = response.answer.length <= 8000 ? response.answer : `${response.answer.slice(0, 7999)}…`;\n  const message = emptyConversationMessage('assistant', answer, id, createdAt);\n  return {\n    ...message,\n    mode: response.mode,\n    answerSource: response.answer_source,\n    sourceCount: Math.max(response.evidence_ids.length, response.results.length),\n    missingFields: response.missing_fields.slice(0, 20).map((field) => humanizeIdentifier(field).slice(0, 160)),\n    proposal: mapProposal(response)\n  };\n}\n", 'frontend/src/app/features/assistant-conversation/assistant-composer/assistant-composer.component.html': '<div class="composer" [class.composer--disabled]="disabled">\n  @if (clarificationActive) {\n    <div class="composer__context">\n      <span><strong>Clarification mode</strong>Your reply will include the original request context.</span>\n      <button type="button" (click)="clarificationCancelled.emit()">Start a new request</button>\n    </div>\n  }\n  <label class="sr-only" for="assistant-composer-input">Ask the workplace agent</label>\n  <textarea\n    #editor\n    id="assistant-composer-input"\n    rows="3"\n    [value]="draft()"\n    [disabled]="disabled || pending"\n    [attr.maxlength]="maxLength"\n    [attr.aria-describedby]="\'assistant-composer-help assistant-composer-count\'"\n    placeholder="Ask about users, reports, access, settings, approvals, or audit activity…"\n    (input)="update($event)"\n    (keydown)="keydown($event)"\n  ></textarea>\n  <div class="composer__footer">\n    <span id="assistant-composer-help">Enter to send · Shift+Enter for a new line</span>\n    <span id="assistant-composer-count" [class.composer__count--warning]="remaining < 200">{{ remaining }} remaining</span>\n    @if (pending) {\n      <app-ui-button variant="outline" size="small" (pressed)="stopRequested.emit()">Stop waiting</app-ui-button>\n    } @else {\n      <app-ui-button size="small" [disabled]="!canSend" (pressed)="submit()">Send</app-ui-button>\n    }\n  </div>\n</div>\n', 'frontend/src/app/features/assistant-conversation/assistant-composer/assistant-composer.component.scss': ':host { display: block; }\n.composer { display: grid; gap: var(--ui-space-2); border: 1px solid var(--ui-border-default); border-radius: var(--ui-radius-lg); background: var(--ui-surface-base); padding: var(--ui-space-2); box-shadow: var(--ui-shadow-xs); }\n.composer:focus-within { border-color: var(--ui-border-focus); box-shadow: 0 0 0 3px color-mix(in srgb, var(--ui-border-focus) 18%, transparent); }\n.composer--disabled { opacity: 0.65; }\n.composer textarea { width: 100%; min-height: 4.5rem; resize: vertical; border: 0; outline: 0; background: transparent; color: var(--ui-text-primary); padding: var(--ui-space-2); line-height: var(--ui-leading-normal); }\n.composer textarea::placeholder { color: var(--ui-text-muted); }\n.composer__context { display: flex; align-items: center; justify-content: space-between; gap: var(--ui-space-3); border-radius: var(--ui-radius-md); background: var(--ui-status-warning-bg); color: var(--ui-text-secondary); padding: var(--ui-space-2) var(--ui-space-3); font-size: var(--ui-text-xs); }\n.composer__context span { display: grid; }\n.composer__context button { border: 0; background: transparent; color: var(--ui-text-link); cursor: pointer; font-weight: var(--ui-weight-semibold); }\n.composer__footer { display: grid; grid-template-columns: minmax(0, 1fr) auto auto; align-items: center; gap: var(--ui-space-2); color: var(--ui-text-muted); font-size: var(--ui-text-xs); }\n.composer__count--warning { color: var(--ui-status-warning-fg); font-weight: var(--ui-weight-semibold); }\n@media (max-width: 28rem) { .composer__footer { grid-template-columns: 1fr auto; } .composer__footer > span:first-child { grid-column: 1 / -1; } .composer__context { align-items: flex-start; flex-direction: column; } }\n', 'frontend/src/app/features/assistant-conversation/assistant-composer/assistant-composer.component.spec.ts': "import { TestBed } from '@angular/core/testing';\nimport { describe, expect, it, vi } from 'vitest';\nimport { AssistantComposerComponent } from './assistant-composer.component';\n\ndescribe('AssistantComposerComponent', () => {\n  it('submits on Enter, preserves Shift+Enter, and clears the draft', async () => {\n    await TestBed.configureTestingModule({ imports:[AssistantComposerComponent] }).compileComponents();\n    const fixture=TestBed.createComponent(AssistantComposerComponent);\n    const submitted=vi.fn();\n    fixture.componentInstance.submitted.subscribe(submitted);\n    fixture.componentInstance.draft.set('Show active users');\n    fixture.detectChanges();\n    fixture.componentInstance.keydown(new KeyboardEvent('keydown',{key:'Enter'}));\n    expect(submitted).toHaveBeenCalledWith('Show active users');\n    expect(fixture.componentInstance.draft()).toBe('');\n  });\n});\n", 'frontend/src/app/features/assistant-conversation/assistant-composer/assistant-composer.component.ts': "import { ChangeDetectionStrategy, Component, EventEmitter, Input, Output, ViewChild, signal, type ElementRef } from '@angular/core';\nimport { UiButtonComponent } from '../../../shared/ui';\n\n@Component({\n  selector: 'app-assistant-composer',\n  standalone: true,\n  imports: [UiButtonComponent],\n  changeDetection: ChangeDetectionStrategy.OnPush,\n  templateUrl: './assistant-composer.component.html',\n  styleUrl: './assistant-composer.component.scss'\n})\nexport class AssistantComposerComponent {\n  @ViewChild('editor') private editor?: ElementRef<HTMLTextAreaElement>;\n  @Input() disabled = false;\n  @Input() pending = false;\n  @Input() maxLength = 4000;\n  @Input() clarificationActive = false;\n  @Output() readonly submitted = new EventEmitter<string>();\n  @Output() readonly stopRequested = new EventEmitter<void>();\n  @Output() readonly clarificationCancelled = new EventEmitter<void>();\n  readonly draft = signal('');\n\n  get remaining(): number { return this.maxLength - this.draft().length; }\n  get canSend(): boolean { return !this.disabled && !this.pending && this.draft().trim().length > 0 && this.remaining >= 0; }\n\n  update(event: Event): void {\n    this.draft.set((event.target as HTMLTextAreaElement).value);\n  }\n\n  keydown(event: KeyboardEvent): void {\n    if (event.key === 'Enter' && !event.shiftKey && !event.isComposing) {\n      event.preventDefault();\n      this.submit();\n    }\n  }\n\n  submit(): void {\n    if (!this.canSend) return;\n    this.submitted.emit(this.draft().trim());\n    this.draft.set('');\n  }\n\n  focus(): void {\n    this.editor?.nativeElement.focus();\n  }\n}\n", 'frontend/src/app/features/assistant-conversation/assistant-message/assistant-message.component.html': '<article class="message message--{{ message.role }}" [attr.aria-label]="message.role === \'user\' ? \'You\' : \'Workplace agent\'">\n  <div class="message__avatar" aria-hidden="true">{{ message.role === \'user\' ? \'Y\' : message.role === \'error\' ? \'!\' : \'W\' }}</div>\n  <div class="message__content">\n    <div class="message__meta">\n      <strong>{{ message.role === \'user\' ? \'You\' : message.role === \'error\' ? \'Request error\' : message.role === \'notice\' ? \'Notice\' : \'Workplace agent\' }}</strong>\n      <time [attr.datetime]="message.createdAt">{{ message.createdAt | date:\'shortTime\' }}</time>\n    </div>\n\n    @if (message.role === \'error\') {\n      <app-ui-callout tone="danger" [title]="message.title ?? \'Request failed\'">\n        {{ message.text }}\n      </app-ui-callout>\n      @if (message.retryable && retryAvailable) {\n        <div class="message__actions">\n          <app-ui-button variant="outline" size="small" (pressed)="retryRequested.emit()">Retry manually</app-ui-button>\n          <small>A retry may prepare another proposal. Check Pending approvals if the earlier request may have completed.</small>\n        </div>\n      }\n    } @else if (message.role === \'notice\') {\n      <app-ui-callout [tone]="message.tone === \'warning\' ? \'warning\' : \'info\'" [title]="message.title ?? \'Notice\'">\n        {{ message.text }}\n      </app-ui-callout>\n    } @else {\n      <p class="message__text">{{ message.text }}</p>\n      @if (message.contextNote) { <p class="message__context">{{ message.contextNote }}</p> }\n      @if (message.role === \'assistant\' && message.sourceCount > 0) {\n        <app-ui-badge tone="info">{{ sourceLabel() }}</app-ui-badge>\n      }\n      @if (message.mode === \'clarification_required\') {\n        <div class="clarification-card">\n          <strong>More detail is required</strong>\n          <p>Your next message will be sent together with the original request.</p>\n          @if (message.missingFields.length) {\n            <div class="clarification-card__fields" aria-label="Missing details">\n              @for (field of message.missingFields; track field) { <app-ui-badge tone="warning">{{ field }}</app-ui-badge> }\n            </div>\n          }\n        </div>\n      }\n      @if (message.proposal; as proposal) {\n        <app-assistant-proposal-card [proposal]="proposal" (reviewRequested)="reviewProposal.emit()" />\n      }\n    }\n  </div>\n</article>\n', 'frontend/src/app/features/assistant-conversation/assistant-message/assistant-message.component.scss': ':host { display: block; }\n.message { display: grid; grid-template-columns: 2rem minmax(0, 1fr); gap: var(--ui-space-3); padding: var(--ui-space-3) 0; }\n.message__avatar { display: inline-grid; width: 2rem; height: 2rem; place-items: center; border-radius: var(--ui-radius-pill); background: var(--ui-surface-muted); color: var(--ui-text-secondary); font-size: var(--ui-text-xs); font-weight: var(--ui-weight-bold); }\n.message--assistant .message__avatar { background: var(--ui-brand-soft); color: var(--ui-neutral-900); }\n.message--error .message__avatar { background: var(--ui-status-danger-bg); color: var(--ui-status-danger-fg); }\n.message__content { display: grid; min-width: 0; gap: var(--ui-space-2); }\n.message__meta { display: flex; align-items: baseline; justify-content: space-between; gap: var(--ui-space-3); font-size: var(--ui-text-xs); }\n.message__meta time { color: var(--ui-text-muted); }\n.message__text { margin: 0; color: var(--ui-text-primary); line-height: var(--ui-leading-relaxed); white-space: pre-wrap; overflow-wrap: anywhere; }\n.message__context { margin: 0; color: var(--ui-text-muted); font-size: var(--ui-text-xs); }\n.message__actions { display: grid; justify-items: start; gap: var(--ui-space-2); }\n.message__actions small { color: var(--ui-text-muted); line-height: var(--ui-leading-normal); }\n.clarification-card { display: grid; gap: var(--ui-space-2); border: 1px solid var(--ui-status-warning-fg); border-radius: var(--ui-radius-md); background: var(--ui-status-warning-bg); padding: var(--ui-space-3); }\n.clarification-card p { margin: 0; color: var(--ui-text-secondary); font-size: var(--ui-text-xs); }\n.clarification-card__fields { display: flex; flex-wrap: wrap; gap: var(--ui-space-2); }\n', 'frontend/src/app/features/assistant-conversation/assistant-message/assistant-message.component.ts': "import { DatePipe } from '@angular/common';\nimport { ChangeDetectionStrategy, Component, EventEmitter, Input, Output } from '@angular/core';\nimport { UiBadgeComponent, UiButtonComponent, UiCalloutComponent } from '../../../shared/ui';\nimport type { ConversationMessage } from '../agent-conversation.model';\nimport { AssistantProposalCardComponent } from '../assistant-proposal-card/assistant-proposal-card.component';\n\n@Component({\n  selector: 'app-assistant-message',\n  standalone: true,\n  imports: [DatePipe, UiBadgeComponent, UiButtonComponent, UiCalloutComponent, AssistantProposalCardComponent],\n  changeDetection: ChangeDetectionStrategy.OnPush,\n  templateUrl: './assistant-message.component.html',\n  styleUrl: './assistant-message.component.scss'\n})\nexport class AssistantMessageComponent {\n  @Input({ required: true }) message!: ConversationMessage;\n  @Input() retryAvailable = false;\n  @Output() readonly retryRequested = new EventEmitter<void>();\n  @Output() readonly reviewProposal = new EventEmitter<void>();\n\n  sourceLabel(): string {\n    return `${this.message.sourceCount} verified ${this.message.sourceCount === 1 ? 'source' : 'sources'}`;\n  }\n}\n", 'frontend/src/app/features/assistant-conversation/assistant-proposal-card/assistant-proposal-card.component.html': '<section class="proposal-card" aria-label="Governed action proposal">\n  <div class="proposal-card__heading">\n    <div>\n      <p>Review required</p>\n      <h3>{{ proposal.actionLabel }}</h3>\n    </div>\n    <app-ui-badge [tone]="riskTone()">{{ proposal.riskLevel }} risk</app-ui-badge>\n  </div>\n\n  <div class="proposal-card__meta">\n    <span>Status: {{ proposal.statusLabel }}</span>\n    <span>Expires {{ proposal.expiresAt | date:\'medium\' }}</span>\n  </div>\n\n  @if (proposal.changes.length) {\n    <div class="proposal-card__changes">\n      @for (change of proposal.changes; track change.fieldLabel + $index) {\n        <div class="proposal-change">\n          <strong>{{ change.fieldLabel }}</strong>\n          <span><small>Current</small>{{ change.beforeSummary }}</span>\n          <span><small>Proposed</small>{{ change.afterSummary }}</span>\n        </div>\n      }\n    </div>\n  }\n\n  <p class="proposal-card__notice">This screen has not executed the change. Review and approval remain separate governed steps.</p>\n  <app-ui-button variant="outline" [fullWidth]="true" (pressed)="reviewRequested.emit()">Review pending approvals</app-ui-button>\n</section>\n', 'frontend/src/app/features/assistant-conversation/assistant-proposal-card/assistant-proposal-card.component.scss': ':host { display: block; }\n.proposal-card { display: grid; gap: var(--ui-space-3); border: 1px solid var(--ui-border-default); border-radius: var(--ui-radius-lg); background: var(--ui-surface-subtle); padding: var(--ui-space-4); }\n.proposal-card__heading { display: flex; align-items: flex-start; justify-content: space-between; gap: var(--ui-space-3); }\n.proposal-card__heading p { margin: 0 0 var(--ui-space-1); color: var(--ui-text-muted); font-size: var(--ui-text-xs); font-weight: var(--ui-weight-semibold); letter-spacing: 0.06em; text-transform: uppercase; }\n.proposal-card__heading h3 { margin: 0; font-size: var(--ui-text-md); }\n.proposal-card__meta { display: flex; flex-wrap: wrap; gap: var(--ui-space-2) var(--ui-space-4); color: var(--ui-text-secondary); font-size: var(--ui-text-xs); }\n.proposal-card__changes { display: grid; gap: var(--ui-space-2); }\n.proposal-change { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: var(--ui-space-2); border-top: 1px solid var(--ui-border-subtle); padding-top: var(--ui-space-2); font-size: var(--ui-text-xs); }\n.proposal-change strong { grid-column: 1 / -1; }\n.proposal-change span { display: grid; gap: var(--ui-space-1); min-width: 0; overflow-wrap: anywhere; }\n.proposal-change small { color: var(--ui-text-muted); }\n.proposal-card__notice { margin: 0; color: var(--ui-text-secondary); font-size: var(--ui-text-xs); }\n@media (max-width: 24rem) { .proposal-change { grid-template-columns: 1fr; } .proposal-change strong { grid-column: auto; } }\n', 'frontend/src/app/features/assistant-conversation/assistant-proposal-card/assistant-proposal-card.component.ts': "import { DatePipe } from '@angular/common';\nimport { ChangeDetectionStrategy, Component, EventEmitter, Input, Output } from '@angular/core';\nimport { UiBadgeComponent, UiButtonComponent } from '../../../shared/ui';\nimport type { ConversationProposal } from '../agent-conversation.model';\n\n@Component({\n  selector: 'app-assistant-proposal-card',\n  standalone: true,\n  imports: [DatePipe, UiBadgeComponent, UiButtonComponent],\n  changeDetection: ChangeDetectionStrategy.OnPush,\n  templateUrl: './assistant-proposal-card.component.html',\n  styleUrl: './assistant-proposal-card.component.scss'\n})\nexport class AssistantProposalCardComponent {\n  @Input({ required: true }) proposal!: ConversationProposal;\n  @Output() readonly reviewRequested = new EventEmitter<void>();\n\n  riskTone(): 'info' | 'warning' | 'danger' {\n    return this.proposal.riskLevel === 'high' ? 'danger' : this.proposal.riskLevel === 'medium' ? 'warning' : 'info';\n  }\n}\n", 'scripts/validate_angular_phase4.py': '#!/usr/bin/env python3\nfrom __future__ import annotations\nimport argparse, json\nfrom pathlib import Path\n\nREQUIRED=(\n \'frontend/src/app/features/assistant-conversation/agent-conversation.model.ts\',\n \'frontend/src/app/features/assistant-conversation/agent-response.mapper.ts\',\n \'frontend/src/app/features/assistant-conversation/agent-conversation.store.ts\',\n \'frontend/src/app/features/assistant-conversation/assistant-composer/assistant-composer.component.ts\',\n \'frontend/src/app/features/assistant-conversation/assistant-message/assistant-message.component.ts\',\n \'frontend/src/app/features/assistant-conversation/assistant-proposal-card/assistant-proposal-card.component.ts\',\n \'frontend/e2e/assistant-conversation.spec.ts\',\n \'frontend/docs/PHASE_4_CONVERSATION.md\',\n \'frontend/docs/PHASE_4_ACCEPTANCE.md\',\n)\n\ndef validate(repo:Path)->list[str]:\n errors=[]\n for rel in REQUIRED:\n  if not (repo/rel).is_file(): errors.append(f\'{rel}: missing\')\n package=json.loads((repo/\'frontend/package.json\').read_text(encoding=\'utf-8\'))\n if \'validate:phase4\' not in package.get(\'scripts\',{}): errors.append(\'package.json: validate:phase4 missing\')\n store=(repo/\'frontend/src/app/features/assistant-conversation/agent-conversation.store.ts\').read_text(encoding=\'utf-8\')\n for required in (\'WorkplaceAgentApiService\',\'sessionStorage\',\'composeClarificationQuery\',\'CLARIFICATION_REPLY_LIMIT\',\'stopWaiting\',\'retryLast\'):\n  if required not in store: errors.append(f\'conversation store missing {required}\')\n for forbidden in (\'HttpClient\',\'EventSource\',\'WebSocket\',\'setInterval(\',\'setTimeout(\'):\n  if forbidden in store: errors.append(f\'conversation store contains forbidden direct transport or fake timing: {forbidden}\')\n mapper=(repo/\'frontend/src/app/features/assistant-conversation/agent-response.mapper.ts\').read_text(encoding=\'utf-8\')\n for forbidden in (\'evidence_ids:\', \'proposal.id\', \'organization_id:\'):\n  if forbidden in mapper: errors.append(f\'response mapper may expose internal identifier: {forbidden}\')\n panel=(repo/\'frontend/src/app/layout/assistant-panel/assistant-panel.component.html\').read_text(encoding=\'utf-8\')\n for required in (\'app-assistant-composer\',\'app-assistant-message\',\'role="log"\',\'Waiting for the backend response\'):\n  if required not in panel: errors.append(f\'assistant panel missing {required}\')\n for forbidden in (\'chain-of-thought\',\'proposal_internal\',\'evidence_internal\',\'organizationId\',\'currentUser.userId()\'):\n  if forbidden in panel: errors.append(f\'assistant panel exposes forbidden value: {forbidden}\')\n shell=(repo/\'frontend/src/app/layout/app-shell/app-shell.component.html\').read_text(encoding=\'utf-8\')\n if \'[currentSection]="state.activeSection()"\' not in shell: errors.append(\'app shell does not pass current workspace context to Ask AI\')\n e2e=(repo/\'frontend/e2e/assistant-conversation.spec.ts\').read_text(encoding=\'utf-8\')\n for scenario in (\'real REST query\',\'clarification context\',\'governed proposal review\'):\n  if scenario not in e2e: errors.append(f\'Playwright missing {scenario}\')\n visible=\'\\n\'.join(path.read_text(encoding=\'utf-8\') for path in (repo/\'frontend/src/app\').rglob(\'*.html\'))\n for forbidden in (\'Cloudflare\',\'proposal_internal\',\'evidence_internal\',\'/agent/query\'):\n  if forbidden in visible: errors.append(f\'visible templates contain forbidden implementation detail: {forbidden}\')\n return errors\n\ndef main()->int:\n parser=argparse.ArgumentParser();parser.add_argument(\'--repo\',default=\'.\');args=parser.parse_args();errors=validate(Path(args.repo).resolve())\n if errors:\n  print(\'Angular Phase 4 validation failed:\');[print(f\'- {e}\') for e in errors];return 1\n print(\'Angular Phase 4 conversation is valid: REST responses, clarification context, safe proposals, and session boundaries pass.\');return 0\nif __name__==\'__main__\': raise SystemExit(main())\n', 'tests/test_angular_phase4.py': "from __future__ import annotations\nfrom pathlib import Path\nfrom scripts.validate_angular_phase4 import validate\nROOT=Path(__file__).resolve().parents[1]\n\ndef test_phase4_conversation_contract()->None:\n assert validate(ROOT)==[]\n\ndef test_phase4_does_not_claim_streaming_or_server_history()->None:\n docs=(ROOT/'frontend/docs/PHASE_4_CONVERSATION.md').read_text(encoding='utf-8')\n assert 'does not expose a conversation ID' in docs\n assert 'never fabricates intermediate reasoning' in docs\n assert 'sessionStorage' in docs\n"}
APPENDS = {'README.md': '<!-- ANGULAR_FRONTEND_PHASE_4_CONVERSATION -->\n## Angular frontend Phase 4 conversation\n\nAsk AI now submits real requests through the existing governed REST agent endpoint and renders read answers, clarification requests, and reviewable action proposals. Current-tab history is normalized and session-scoped; no streaming or server-side conversation persistence is claimed. See `frontend/docs/PHASE_4_CONVERSATION.md`.\n', 'frontend/README.md': '<!-- ANGULAR_FRONTEND_PHASE_4_CONVERSATION -->\n## Phase 4 Ask AI conversation\n\nThe responsive Ask AI panel is connected to `WorkplaceAgentApiService.query`. It renders all three current backend response modes, keeps only normalized current-tab history, and never invents streaming or hidden reasoning.\n', 'APPLY_AND_VALIDATE.md': '<!-- ANGULAR_FRONTEND_PHASE_4_VALIDATION -->\n## Angular frontend Phase 4 validation\n\n```powershell\npython scripts/validate_angular_phase4.py --repo .\npytest -q tests/test_angular_phase4.py\nSet-Location frontend\nnpm run validate:phase4\nnpm run e2e\nSet-Location ..\npytest -q\ngit diff --check\n```\n'}
GENERATED_PATHS = (
    "frontend/.angular",
    "frontend/dist",
    "frontend/coverage",
    "frontend/playwright-report",
    "frontend/test-results",
)


def run(repo: Path, *args: str, check: bool = True) -> subprocess.CompletedProcess[str]:
    return subprocess.run(
        list(args),
        cwd=repo,
        check=check,
        text=True,
        stdout=subprocess.PIPE,
        stderr=subprocess.STDOUT,
    )


def git(repo: Path, *args: str) -> str:
    return run(repo, "git", *args).stdout.strip()


def git_blob_sha(data: bytes) -> str:
    header = f"blob {len(data)}\0".encode("ascii")
    return hashlib.sha1(header + data, usedforsecurity=False).hexdigest()


def npm_command() -> str:
    return "npm.cmd" if os.name == "nt" else "npm"


def normalize_origin(origin: str) -> str:
    normalized = origin.strip().removesuffix(".git")
    if normalized.startswith("git@github.com:"):
        normalized = "https://github.com/" + normalized[len("git@github.com:"):]
    return normalized.rstrip("/")


def verify_repo(repo: Path) -> None:
    root = Path(git(repo, "rev-parse", "--show-toplevel")).resolve()
    if root != repo.resolve():
        raise RuntimeError("Run from the repository root or pass --repo.")
    branch = git(repo, "branch", "--show-current")
    if branch != EXPECTED_BRANCH:
        raise RuntimeError(f"Expected branch {EXPECTED_BRANCH}, found {branch}.")
    head = git(repo, "rev-parse", "HEAD")
    if head != EXPECTED_HEAD:
        raise RuntimeError(f"Expected HEAD {EXPECTED_HEAD}, found {head}.")
    origin = git(repo, "remote", "get-url", "origin")
    if not normalize_origin(origin).endswith(EXPECTED_REPOSITORY):
        raise RuntimeError(f"Unexpected origin: {origin}")
    tracked = git(repo, "status", "--porcelain", "--untracked-files=no")
    if tracked:
        raise RuntimeError("Tracked working tree must be clean before applying Phase 4.")

    for relative, expected in EXPECTED_BLOBS.items():
        path = repo / relative
        if not path.is_file():
            raise RuntimeError(f"Missing Phase 3 source file: {relative}")
        actual = git_blob_sha(path.read_bytes())
        if actual != expected:
            raise RuntimeError(f"Phase 3 source changed: {relative} (expected blob {expected}, found {actual})")

    for relative in NEW_FILES:
        if (repo / relative).exists():
            raise RuntimeError(f"Phase 4 target already exists: {relative}")

    for relative, addition in APPENDS.items():
        path = repo / relative
        if not path.is_file():
            raise RuntimeError(f"Missing documentation target: {relative}")
        marker = next((line for line in addition.splitlines() if line.startswith("<!--")), "")
        if marker and marker in path.read_text(encoding="utf-8"):
            raise RuntimeError(f"Phase 4 marker already exists in {relative}")

    required_baseline = (
        "scripts/validate_frontend_contracts.py",
        "scripts/validate_angular_phase1.py",
        "scripts/validate_angular_phase2.py",
        "scripts/validate_angular_phase3.py",
        "frontend/package-lock.json",
        "frontend/src/app/core/api/workplace-agent-api.service.ts",
        "frontend/src/app/core/api/wire.schemas.ts",
    )
    for relative in required_baseline:
        if not (repo / relative).is_file():
            raise RuntimeError(f"Required validated baseline file is missing: {relative}")

    prechecks = (
        (sys.executable, "scripts/validate_frontend_contracts.py", "--repo", "."),
        (sys.executable, "scripts/validate_angular_phase1.py", "--repo", "."),
        (sys.executable, "scripts/validate_angular_phase2.py", "--repo", "."),
        (sys.executable, "scripts/validate_angular_phase3.py", "--repo", "."),
    )
    for command in prechecks:
        result = run(repo, *command, check=False)
        if result.returncode != 0:
            raise RuntimeError("Baseline validation failed: " + " ".join(command) + "\n" + result.stdout)


def write_atomic(path: Path, content: str) -> None:
    path.parent.mkdir(parents=True, exist_ok=True)
    temporary = path.with_name(path.name + ".phase4.tmp")
    temporary.write_text(content.rstrip() + "\n", encoding="utf-8", newline="\n")
    os.replace(temporary, path)


def cleanup_generated(repo: Path) -> None:
    for relative in GENERATED_PATHS:
        path = repo / relative
        if path.is_dir():
            shutil.rmtree(path, ignore_errors=True)
        elif path.exists():
            path.unlink()


def validate_after_apply(
    repo: Path,
    *,
    skip_node: bool,
    skip_browser_tests: bool,
    skip_full_backend: bool,
) -> None:
    checks = [
        (sys.executable, "scripts/validate_frontend_contracts.py", "--repo", "."),
        (sys.executable, "scripts/validate_angular_phase1.py", "--repo", "."),
        (sys.executable, "scripts/validate_angular_phase2.py", "--repo", "."),
        (sys.executable, "scripts/validate_angular_phase3.py", "--repo", "."),
        (sys.executable, "scripts/validate_angular_phase4.py", "--repo", "."),
        (
            sys.executable,
            "-m",
            "pytest",
            "-q",
            "tests/test_frontend_contracts.py",
            "tests/test_angular_phase1.py",
            "tests/test_angular_phase2.py",
            "tests/test_angular_phase3.py",
            "tests/test_angular_phase4.py",
        ),
        (sys.executable, "-m", "compileall", "-q", "scripts", "tests"),
        ("git", "diff", "--check"),
    ]
    for command in checks:
        result = run(repo, *command, check=False)
        if result.returncode != 0:
            raise RuntimeError("Post-apply validation failed: " + " ".join(command) + "\n" + result.stdout)

    if not skip_node:
        frontend = repo / "frontend"
        npm = npm_command()
        if not (frontend / "node_modules").is_dir():
            install = run(frontend, npm, "ci", check=False)
            if install.returncode != 0:
                raise RuntimeError("npm ci failed:\n" + install.stdout)
        angular = run(frontend, npm, "run", "validate:phase4", check=False)
        if angular.returncode != 0:
            raise RuntimeError("Angular Phase 4 validation failed:\n" + angular.stdout)
        if not skip_browser_tests:
            browser = run(frontend, npm, "run", "e2e", check=False)
            if browser.returncode != 0:
                raise RuntimeError("Playwright Phase 4 tests failed:\n" + browser.stdout)

    if not skip_full_backend:
        backend = run(repo, sys.executable, "-m", "pytest", "-q", check=False)
        if backend.returncode != 0:
            raise RuntimeError("Complete backend regression suite failed:\n" + backend.stdout)


def apply(
    repo: Path,
    *,
    skip_node: bool,
    skip_browser_tests: bool,
    skip_full_backend: bool,
) -> None:
    created: list[Path] = []
    backups: dict[Path, bytes] = {}
    node_modules_created = not (repo / "frontend/node_modules").exists()
    try:
        for relative, content in REPLACEMENTS.items():
            path = repo / relative
            backups[path] = path.read_bytes()
            write_atomic(path, content)
        for relative, content in NEW_FILES.items():
            path = repo / relative
            write_atomic(path, content)
            created.append(path)
        for relative, addition in APPENDS.items():
            path = repo / relative
            backups[path] = path.read_bytes()
            original = path.read_text(encoding="utf-8").rstrip()
            write_atomic(path, original + "\n\n" + addition.strip())

        validate_after_apply(
            repo,
            skip_node=skip_node,
            skip_browser_tests=skip_browser_tests,
            skip_full_backend=skip_full_backend,
        )
    except Exception:
        for path, content in backups.items():
            path.parent.mkdir(parents=True, exist_ok=True)
            path.write_bytes(content)
        for path in reversed(created):
            if path.exists():
                path.unlink()
        for directory in sorted({path.parent for path in created}, key=lambda item: len(item.parts), reverse=True):
            try:
                directory.rmdir()
            except OSError:
                pass
        cleanup_generated(repo)
        if node_modules_created:
            shutil.rmtree(repo / "frontend/node_modules", ignore_errors=True)
        raise


def main() -> int:
    parser = argparse.ArgumentParser()
    parser.add_argument("--repo", default=".")
    parser.add_argument("--skip-node", action="store_true", help="Skip npm, Angular, and Playwright validation; not valid for final acceptance.")
    parser.add_argument("--skip-browser-tests", action="store_true", help="Skip actual Playwright execution; not valid for final acceptance.")
    parser.add_argument("--skip-full-backend", action="store_true", help="Skip the complete backend pytest suite; not valid for final acceptance.")
    args = parser.parse_args()
    repo = Path(args.repo).resolve()
    verify_repo(repo)
    apply(
        repo,
        skip_node=args.skip_node,
        skip_browser_tests=args.skip_browser_tests,
        skip_full_backend=args.skip_full_backend,
    )
    print("Applied Angular Phase 4 real Ask AI conversation experience.")
    print(f"Baseline: {EXPECTED_HEAD}")
    print(f"Replaced {len(REPLACEMENTS)} files, created {len(NEW_FILES)} files, and updated {len(APPENDS)} documents.")
    print("No files were staged, committed, or pushed.")
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
